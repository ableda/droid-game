describe('Target Service', () => {
  describe('getNextTargetToDestroy', () => {
    it('filters radarScans based on protocol commands', () => {
      // TODO: implement tests
      expect(true).toBe(false);
    });

    it('finds closest enemy in filtered radarScans if closestEnemies protocol');

    it('finds furthest enemy in filtered radarScans if furthestEnemies protocol');
  });
});
