describe('PostRadarQuery Controller', () => {
  it('intializes a target service with radarQuery', () => {
    // TODO: implement tests
    expect(true).toBe(false);
  });

  it('calls findTarget to destroy method');

  it('sends errors to middleware if something goes wrong');
});
